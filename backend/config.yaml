server:
  port: 8080
  mode: debug

database:
  path: ./data/fan-web.db

# jwt.secret 留空：首次初始化会自动生成随机密钥并写回本文件。
# 使用公开默认密钥的旧实例升级后会被自动轮换，旧会话需要重新登录一次。
jwt:
  secret: ""
  expire: 168h

# admin 段不保存明文密码；管理员密码只以数据库 bcrypt 哈希形式存在。
admin:
  username: admin

video:
  root_path: ../anime
