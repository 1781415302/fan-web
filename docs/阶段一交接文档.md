# 阶段一交接文档：项目搭建与基础设施

## 1. 阶段目标

搭建前后端项目骨架，初始化数据库，完成基础布局，使系统可启动运行。

**本阶段完成后系统状态**：
- 后端 Go 服务可启动，`GET /api/health` 返回正常
- 前端 Vue 服务可启动，页面展示基础布局（顶部导航栏 + 内容区）
- SQLite 数据库文件创建成功，4 张表结构就绪
- 首次启动自动创建管理员账号
- 前端可通过 Vite 代理访问后端 API

**本阶段不包含**：登录功能、任何业务页面、番剧管理、视频播放

---

## 2. 环境准备

### 2.1 开发环境

项目根目录：`/home/bishe/work/fan-web/`（WSL Ubuntu 20.04）

需安装以下工具（若未安装）：

```bash
# 检查 Go 版本（需要 1.21+）
go version

# 检查 Node.js 版本（需要 18+）
node --version

# 检查 npm 版本
npm --version
```

如果 Go 未安装：
```bash
wget https://go.dev/dl/go1.22.5.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.22.5.linux-amd64.tar.gz
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
source ~/.bashrc
```

如果 Node.js 未安装（推荐用 nvm）：
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 20
```

### 2.2 现有文件

项目目录中已存在：
- `docs/` — 需求文档和开发计划
- `anime/` — 测试用番剧文件，内有一个 MKV 文件：
  ```
  [CheeseAni] Re：Zero kara Hajimeru Isekai Seikatsu 4th season Soshitsu-Hen [67][CR-WebRip HEVC AAC SRT][简繁内封].mkv
  ```
  此文件供后续阶段测试用，本阶段不需要处理。

---

## 3. 全局技术约定

以下约定在所有阶段通用，后续阶段继承。

### 3.1 统一 API 响应格式

所有接口统一返回以下 JSON 结构，HTTP 状态码始终为 200，用 body 中的 `code` 字段区分业务状态：

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 0 表示成功，非 0 表示错误 |
| message | string | 人类可读的提示信息 |
| data | any \| null | 业务数据，错误时为 null |

### 3.2 错误码定义

| 错误码 | 含义 | 使用场景 |
|--------|------|---------|
| 0 | 成功 | 所有成功响应 |
| 1001 | 请求参数错误 | 参数缺失、格式不对 |
| 1002 | 资源不存在 | 查询的记录不存在 |
| 2001 | 未登录 | 未携带或 Token 无效 |
| 2002 | 无权限 | 非管理员访问管理接口 |
| 2003 | 登录失败 | 用户名或密码错误 |
| 2004 | 用户名已存在 | 创建用户时冲突 |
| 9999 | 服务器内部错误 | 未预期的异常 |

### 3.3 命名规范

- **后端**：Go 标准命名，包名小写，导出类型首字母大写（如 `User`, `Anime`），文件名小写下划线（如 `health.go`）
- **前端**：Vue 组件文件名大驼峰（如 `AppLayout.vue`），普通 ts 文件小写（如 `index.ts`），CSS 类名小写中划线（如 `nav-bar`）
- **数据库**：表名小写复数（`users`, `animes`, `episodes`, `watch_progress`），字段名小写下划线（如 `created_at`）
- **API 路径**：小写，RESTful 风格，统一前缀 `/api`

### 3.4 前后端开发端口

| 服务 | 端口 |
|------|------|
| Go 后端 | 8080 |
| Vue 前端（Vite dev） | 5173 |

前端通过 Vite proxy 将 `/api` 请求代理到 `http://localhost:8080`，开发时无跨域问题。

---

## 4. 后端详细设计

### 4.1 目录结构

在项目根目录下创建 `backend/` 目录，结构如下：

```
backend/
├── main.go                  # 程序入口
├── config.yaml              # 配置文件
├── config/
│   └── config.go            # 配置结构体与加载逻辑
├── database/
│   └── db.go                # SQLite 连接、建表、管理员初始化
├── models/
│   └── models.go            # 数据模型结构体
├── handlers/
│   └── health.go            # 健康检查接口
├── middleware/
│   └── cors.go              # CORS 中间件配置
├── utils/
│   └── response.go          # 统一响应工具函数
├── data/                    # 运行时自动创建
│   └── fan-web.db           # SQLite 数据库文件（运行后生成）
├── go.mod
└── go.sum
```

### 4.2 模块初始化与依赖安装

```bash
cd /home/bishe/work/fan-web
mkdir -p backend
cd backend
go mod init fan-web
```

安装依赖：

```bash
# Web 框架
go get github.com/gin-gonic/gin@latest

# CORS 中间件
go get github.com/gin-contrib/cors@latest

# SQLite 驱动（纯 Go 实现，无需 CGO，方便交叉编译）
go get modernc.org/sqlite@latest

# YAML 配置解析
go get gopkg.in/yaml.v3@latest

# 密码哈希
go get golang.org/x/crypto/bcrypt@latest
```

> **技术选型说明**：使用 `modernc.org/sqlite` 而非 `github.com/mattn/go-sqlite3`，因为前者是纯 Go 实现无需 CGO，后续交叉编译部署到 VPS 时更简单（`CGO_ENABLED=0 go build` 即可）。

### 4.3 配置文件 `config.yaml`

在 `backend/config.yaml` 中写入：

```yaml
server:
  port: 8080
  mode: debug  # debug | release

database:
  path: ./data/fan-web.db

jwt:
  secret: "fan-web-secret-key-change-in-production"
  expire: 168h  # 7天

admin:
  username: admin
  password: admin123

video:
  root_path: ../anime  # 视频文件根目录，相对于 backend/ 目录
```

### 4.4 配置加载 `config/config.go`

```go
package config

import (
	"os"
	"time"

	"gopkg.in/yaml.v3"
)

type Config struct {
	Server   ServerConfig   `yaml:"server"`
	Database DatabaseConfig `yaml:"database"`
	JWT      JWTConfig      `yaml:"jwt"`
	Admin    AdminConfig    `yaml:"admin"`
	Video    VideoConfig    `yaml:"video"`
}

type ServerConfig struct {
	Port int    `yaml:"port"`
	Mode string `yaml:"mode"`
}

type DatabaseConfig struct {
	Path string `yaml:"path"`
}

type JWTConfig struct {
	Secret string        `yaml:"secret"`
	Expire time.Duration `yaml:"expire"`
}

type AdminConfig struct {
	Username string `yaml:"username"`
	Password string `yaml:"password"`
}

type VideoConfig struct {
	RootPath string `yaml:"root_path"`
}

// Load 从指定路径加载配置文件
func Load(path string) (*Config, error) {
	data, err := os.ReadFile(path)
	if err != nil {
		return nil, err
	}

	var cfg Config
	if err := yaml.Unmarshal(data, &cfg); err != nil {
		return nil, err
	}

	// 设置默认值
	if cfg.Server.Port == 0 {
		cfg.Server.Port = 8080
	}
	if cfg.Server.Mode == "" {
		cfg.Server.Mode = "debug"
	}
	if cfg.Database.Path == "" {
		cfg.Database.Path = "./data/fan-web.db"
	}
	if cfg.JWT.Secret == "" {
		cfg.JWT.Secret = "default-secret"
	}
	if cfg.JWT.Expire == 0 {
		cfg.JWT.Expire = 168 * time.Hour
	}
	if cfg.Admin.Username == "" {
		cfg.Admin.Username = "admin"
	}
	if cfg.Admin.Password == "" {
		cfg.Admin.Password = "admin123"
	}

	return &cfg, nil
}
```

### 4.5 数据库初始化 `database/db.go`

```go
package database

import (
	"database/sql"
	"fmt"
	"log"
	"os"
	"path/filepath"

	_ "modernc.org/sqlite"
	"golang.org/x/crypto/bcrypt"
)

var DB *sql.DB

// Init 初始化数据库连接并创建表
func Init(dbPath string) error {
	// 确保数据目录存在
	dir := filepath.Dir(dbPath)
	if err := os.MkdirAll(dir, 0755); err != nil {
		return fmt.Errorf("创建数据库目录失败: %w", err)
	}

	// 打开数据库连接
	db, err := sql.Open("sqlite", dbPath)
	if err != nil {
		return fmt.Errorf("打开数据库失败: %w", err)
	}

	// 设置连接池
	db.SetMaxOpenConns(1) // SQLite 写入是串行的，连接数设为 1 避免锁冲突
	db.SetMaxIdleConns(1)

	// 设置 PRAGMA
	pragmas := []string{
		"PRAGMA journal_mode=WAL;",      // WAL 模式，提升并发读性能
		"PRAGMA busy_timeout=5000;",     // 忙等待 5 秒
		"PRAGMA foreign_keys=ON;",       // 启用外键约束
	}
	for _, pragma := range pragmas {
		if _, err := db.Exec(pragma); err != nil {
			return fmt.Errorf("设置 PRAGMA 失败: %w", err)
		}
	}

	// 创建表
	if err := createTables(db); err != nil {
		return fmt.Errorf("创建表失败: %w", err)
	}

	DB = db
	log.Println("数据库初始化成功")
	return nil
}

func createTables(db *sql.DB) error {
	tables := []string{
		`CREATE TABLE IF NOT EXISTS users (
			id INTEGER PRIMARY KEY AUTOINCREMENT,
			username TEXT UNIQUE NOT NULL,
			password TEXT NOT NULL,
			is_admin INTEGER DEFAULT 0,
			created_at DATETIME DEFAULT CURRENT_TIMESTAMP
		);`,
		`CREATE TABLE IF NOT EXISTS animes (
			id INTEGER PRIMARY KEY AUTOINCREMENT,
			title TEXT NOT NULL,
			title_cn TEXT,
			bangumi_id INTEGER,
			cover TEXT,
			summary TEXT,
			ep_count INTEGER DEFAULT 0,
			file_path TEXT,
			created_at DATETIME DEFAULT CURRENT_TIMESTAMP
		);`,
		`CREATE TABLE IF NOT EXISTS episodes (
			id INTEGER PRIMARY KEY AUTOINCREMENT,
			anime_id INTEGER NOT NULL,
			ep_number INTEGER NOT NULL,
			title TEXT,
			file_path TEXT NOT NULL,
			duration INTEGER,
			FOREIGN KEY (anime_id) REFERENCES animes(id) ON DELETE CASCADE
		);`,
		`CREATE TABLE IF NOT EXISTS watch_progress (
			id INTEGER PRIMARY KEY AUTOINCREMENT,
			user_id INTEGER NOT NULL,
			episode_id INTEGER NOT NULL,
			position INTEGER DEFAULT 0,
			watched INTEGER DEFAULT 0,
			updated_at DATETIME,
			FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
			FOREIGN KEY (episode_id) REFERENCES episodes(id) ON DELETE CASCADE
		);`,
		`CREATE UNIQUE INDEX IF NOT EXISTS idx_watch_progress_user_episode
			ON watch_progress(user_id, episode_id);`,
	}

	for _, table := range tables {
		if _, err := db.Exec(table); err != nil {
			return err
		}
	}
	return nil
}

// InitAdmin 首次启动时创建管理员账号
func InitAdmin(username, password string) error {
	// 检查是否已有管理员
	var count int
	err := DB.QueryRow("SELECT COUNT(*) FROM users WHERE is_admin = 1").Scan(&count)
	if err != nil {
		return err
	}
	if count > 0 {
		return nil // 已有管理员，跳过
	}

	// 哈希密码
	hashedPassword, err := bcrypt.GenerateFromPassword([]byte(password), bcrypt.DefaultCost)
	if err != nil {
		return err
	}

	_, err = DB.Exec(
		"INSERT INTO users (username, password, is_admin) VALUES (?, ?, 1)",
		username, string(hashedPassword),
	)
	if err != nil {
		return err
	}

	log.Printf("管理员账号创建成功: %s\n", username)
	return nil
}
```

### 4.6 数据模型 `models/models.go`

```go
package models

import "time"

type User struct {
	ID        int64     `json:"id"`
	Username  string    `json:"username"`
	Password  string    `json:"-"`           // 不序列化到 JSON
	IsAdmin   bool      `json:"is_admin"`
	CreatedAt time.Time `json:"created_at"`
}

type Anime struct {
	ID         int64     `json:"id"`
	Title      string    `json:"title"`
	TitleCn    string    `json:"title_cn"`
	BangumiID  int       `json:"bangumi_id"`
	Cover      string    `json:"cover"`
	Summary    string    `json:"summary"`
	EpCount    int       `json:"ep_count"`
	FilePath   string    `json:"file_path"`
	CreatedAt  time.Time `json:"created_at"`
}

type Episode struct {
	ID       int64  `json:"id"`
	AnimeID  int64  `json:"anime_id"`
	EpNumber int    `json:"ep_number"`
	Title    string `json:"title"`
	FilePath string `json:"file_path"`
	Duration int    `json:"duration"` // 秒
}

type WatchProgress struct {
	ID        int64     `json:"id"`
	UserID    int64     `json:"user_id"`
	EpisodeID int64     `json:"episode_id"`
	Position  int       `json:"position"` // 秒
	Watched   bool      `json:"watched"`
	UpdatedAt time.Time `json:"updated_at"`
}
```

### 4.7 统一响应工具 `utils/response.go`

```go
package utils

import "github.com/gin-gonic/gin"

// Response 统一响应结构
type Response struct {
	Code    int         `json:"code"`
	Message string      `json:"message"`
	Data    interface{} `json:"data"`
}

// Success 返回成功响应
func Success(c *gin.Context, data interface{}) {
	c.JSON(200, Response{
		Code:    0,
		Message: "ok",
		Data:    data,
	})
}

// Error 返回错误响应
func Error(c *gin.Context, code int, message string) {
	c.JSON(200, Response{
		Code:    code,
		Message: message,
		Data:    nil,
	})
}
```

### 4.8 健康检查 `handlers/health.go`

```go
package handlers

import (
	"github.com/gin-gonic/gin"
	"fan-web/utils"
)

// Health 健康检查接口
func Health(c *gin.Context) {
	utils.Success(c, gin.H{
		"status": "ok",
	})
}
```

### 4.9 CORS 中间件 `middleware/cors.go`

```go
package middleware

import (
	"github.com/gin-contrib/cors"
	"github.com/gin-gonic/gin"
)

// CORS 返回 CORS 中间件
func CORS() gin.HandlerFunc {
	return cors.New(cors.Config{
		AllowOrigins:     []string{"http://localhost:5173"},
		AllowMethods:     []string{"GET", "POST", "PUT", "DELETE", "OPTIONS"},
		AllowHeaders:     []string{"Origin", "Content-Type", "Authorization"},
		AllowCredentials: true,
	})
}
```

### 4.10 主程序入口 `main.go`

```go
package main

import (
	"fmt"
	"log"

	"github.com/gin-gonic/gin"
	"fan-web/config"
	"fan-web/database"
	"fan-web/handlers"
	"fan-web/middleware"
)

func main() {
	// 1. 加载配置
	cfg, err := config.Load("config.yaml")
	if err != nil {
		log.Fatalf("加载配置失败: %v", err)
	}

	// 2. 设置 Gin 模式
	gin.SetMode(cfg.Server.Mode)

	// 3. 初始化数据库
	if err := database.Init(cfg.Database.Path); err != nil {
		log.Fatalf("数据库初始化失败: %v", err)
	}

	// 4. 初始化管理员账号
	if err := database.InitAdmin(cfg.Admin.Username, cfg.Admin.Password); err != nil {
		log.Fatalf("管理员初始化失败: %v", err)
	}

	// 5. 创建 Gin 路由
	r := gin.Default()

	// 6. 注册中间件
	r.Use(middleware.CORS())

	// 7. 注册路由
	api := r.Group("/api")
	{
		api.GET("/health", handlers.Health)
	}

	// 8. 启动服务
	addr := fmt.Sprintf(":%d", cfg.Server.Port)
	log.Printf("服务启动，监听 %s\n", addr)
	if err := r.Run(addr); err != nil {
		log.Fatalf("服务启动失败: %v", err)
	}
}
```

### 4.11 后端验证步骤

```bash
cd /home/bishe/work/fan-web/backend
go mod tidy
go run main.go
```

预期输出：
```
[GIN-debug] [WARNING] Creating an Engine instance with the "debug" mode.
...
数据库初始化成功
管理员账号创建成功: admin
服务启动，监听 :8080
```

另开终端测试：
```bash
curl http://localhost:8080/api/health
```

预期返回：
```json
{"code":0,"message":"ok","data":{"status":"ok"}}
```

再次启动应看到（管理员已存在，不再创建）：
```
数据库初始化成功
服务启动，监听 :8080
```

---

## 5. 前端详细设计

### 5.1 目录结构

在项目根目录下创建 `frontend/` 目录，结构如下：

```
frontend/
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
└── src/
    ├── main.ts              # 应用入口
    ├── App.vue              # 根组件
    ├── env.d.ts             # TypeScript 类型声明
    ├── style.css            # 全局样式
    ├── api/
    │   └── index.ts         # Axios 实例
    ├── router/
    │   └── index.ts         # 路由配置
    ├── stores/
    │   └── app.ts           # 全局状态
    ├── components/
    │   └── AppLayout.vue    # 布局组件
    └── views/
        └── HomeView.vue     # 首页
```

### 5.2 项目初始化

```bash
cd /home/bishe/work/fan-web
npm create vite@latest frontend -- --template vue-ts
cd frontend
npm install
npm install vue-router@4 pinia axios
```

### 5.3 Vite 配置 `vite.config.ts`

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
})
```

### 5.4 TypeScript 配置

`tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

`tsconfig.node.json`：

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}
```

### 5.5 TypeScript 类型声明 `src/env.d.ts`

```typescript
/// <reference types="vite/client" />

declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  const component: DefineComponent<{}, {}, any>
  export default component
}
```

### 5.6 Axios 封装 `src/api/index.ts`

```typescript
import axios from 'axios'

const api = axios.create({
  baseURL: '/api',
  timeout: 10000,
})

export default api
```

### 5.7 路由配置 `src/router/index.ts`

```typescript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/',
    name: 'home',
    component: () => import('../views/HomeView.vue'),
  },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

export default router
```

### 5.8 Pinia Store `src/stores/app.ts`

```typescript
import { defineStore } from 'pinia'

export const useAppStore = defineStore('app', {
  state: () => ({
    siteName: '番剧库',
  }),
})
```

### 5.9 布局组件 `src/components/AppLayout.vue`

```vue
<script setup lang="ts">
import { useAppStore } from '../stores/app'

const appStore = useAppStore()
</script>

<template>
  <div class="app-layout">
    <header class="navbar">
      <div class="navbar-inner">
        <div class="navbar-left">
          <router-link to="/" class="logo">{{ appStore.siteName }}</router-link>
        </div>
        <div class="navbar-right">
          <!-- 阶段二将在此处添加用户信息 / 登录按钮 -->
          <span class="nav-placeholder">未登录</span>
        </div>
      </div>
    </header>
    <main class="content">
      <slot></slot>
    </main>
  </div>
</template>

<style scoped>
.app-layout {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.navbar {
  position: sticky;
  top: 0;
  z-index: 100;
  background-color: var(--surface-color);
  border-bottom: 1px solid var(--border-color);
}

.navbar-inner {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 24px;
  height: 56px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.logo {
  font-size: 20px;
  font-weight: 700;
  color: var(--primary-color);
  text-decoration: none;
}

.logo:hover {
  text-decoration: none;
}

.nav-placeholder {
  color: var(--text-secondary);
  font-size: 14px;
}

.content {
  flex: 1;
  max-width: 1400px;
  width: 100%;
  margin: 0 auto;
  padding: 24px;
}

@media (max-width: 768px) {
  .navbar-inner {
    padding: 0 16px;
  }
  .content {
    padding: 16px;
  }
}
</style>
```

### 5.10 首页 `src/views/HomeView.vue`

```vue
<script setup lang="ts">
</script>

<template>
  <div class="home">
    <h1 class="home-title">欢迎使用番剧库</h1>
    <p class="home-desc">系统正在建设中，请期待后续功能。</p>
  </div>
</template>

<style scoped>
.home {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 60vh;
  text-align: center;
}

.home-title {
  font-size: 28px;
  font-weight: 700;
  color: var(--text-color);
  margin-bottom: 12px;
}

.home-desc {
  font-size: 16px;
  color: var(--text-secondary);
}
</style>
```

### 5.11 全局样式 `src/style.css`

```css
:root {
  --bg-color: #0f0f0f;
  --surface-color: #1a1a1a;
  --surface-hover: #2a2a2a;
  --primary-color: #3b82f6;
  --text-color: #e5e7eb;
  --text-secondary: #9ca3af;
  --border-color: #2d2d2d;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
    'Helvetica Neue', Arial, sans-serif;
  background-color: var(--bg-color);
  color: var(--text-color);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

a {
  color: var(--primary-color);
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}
```

### 5.12 应用入口 `src/main.ts`

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import router from './router'
import App from './App.vue'
import './style.css'

const app = createApp(App)
app.use(createPinia())
app.use(router)
app.mount('#app')
```

### 5.13 根组件 `src/App.vue`

```vue
<script setup lang="ts">
import AppLayout from './components/AppLayout.vue'
</script>

<template>
  <AppLayout>
    <router-view />
  </AppLayout>
</template>
```

### 5.14 HTML 入口 `index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>番剧库</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

### 5.15 前端验证步骤

```bash
cd /home/bishe/work/fan-web/frontend
npm run dev
```

浏览器访问 `http://localhost:5173`，应看到：
- 顶部深色导航栏，左侧显示 "番剧库" 蓝色文字
- 右侧显示 "未登录" 灰色文字
- 内容区居中显示 "欢迎使用番剧库" 标题和说明文字

---

## 6. 开发运行流程

日常开发时，需要同时启动前后端：

**终端 1 — 启动后端**：
```bash
cd /home/bishe/work/fan-web/backend
go run main.go
```

**终端 2 — 启动前端**：
```bash
cd /home/bishe/work/fan-web/frontend
npm run dev
```

浏览器访问 `http://localhost:5173` 即可。

前端通过 Vite proxy 将 `/api/*` 请求代理到后端 `http://localhost:8080`，无需关心跨域。

---

## 7. 验收标准

执行者完成后，逐项验证以下标准：

### 后端

- [ ] `backend/` 目录结构符合 4.1 节定义
- [ ] `go build ./...` 无编译错误
- [ ] `go run main.go` 能正常启动
- [ ] 首次启动时输出 "管理员账号创建成功: admin"
- [ ] 第二次启动不再创建管理员
- [ ] `backend/data/fan-web.db` 文件已生成
- [ ] `curl http://localhost:8080/api/health` 返回 `{"code":0,"message":"ok","data":{"status":"ok"}}`
- [ ] 数据库中 users 表有一条 admin 记录，password 字段为 bcrypt 哈希（非明文）
- [ ] 数据库中有 users、animes、episodes、watch_progress 四张表

### 前端

- [ ] `frontend/` 目录结构符合 5.1 节定义
- [ ] `npm run dev` 能正常启动，无报错
- [ ] 浏览器访问 `http://localhost:5173` 显示深色主题页面
- [ ] 顶部导航栏左侧显示 "番剧库"，右侧显示 "未登录"
- [ ] 内容区居中显示欢迎信息
- [ ] 页面在手机宽度下导航栏不溢出（响应式）

### 前后端联调

- [ ] 前端运行时，浏览器开发者工具 Network 中无报错
- [ ] Vite proxy 配置正确，`/api` 请求被代理到后端

---

## 8. 注意事项

### 8.1 SQLite 驱动选择

使用 `modernc.org/sqlite`（纯 Go），**不要**使用 `github.com/mattn/go-sqlite3`（需要 CGO）。原因：
- 纯 Go 实现无需安装 C 编译器
- 交叉编译简单，`CGO_ENABLED=0 go build` 即可
- 对 2-5 人规模的应用性能足够

**驱动名称**是 `"sqlite"`（不是 `"sqlite3"`），`sql.Open` 调用方式：
```go
db, err := sql.Open("sqlite", "./data/fan-web.db")
```

### 8.2 数据库连接池

SQLite 的写入操作是串行的，`SetMaxOpenConns(1)` 是必要的，否则多连接并发写入会导致 `database is locked` 错误。WAL 模式允许同时读取，性能足够。

### 8.3 SQLite 布尔值

SQLite 没有真正的 BOOLEAN 类型，用 INTEGER 0/1 表示。在 Go 代码中读取时，`is_admin` 和 `watched` 字段需要用 `int` 或自定义 Scan 方法处理，不能直接用 `bool`。在 models.go 中定义了 `bool` 类型，后续阶段写 SQL 查询时注意类型转换。

### 8.4 配置文件路径

`config.yaml` 的路径是相对于运行目录的。必须在 `backend/` 目录下执行 `go run main.go`，否则找不到配置文件。

视频根目录 `../anime` 也是相对于 `backend/` 目录，指向项目根目录下的 `anime/` 文件夹。

### 8.5 前端主题

采用**深色主题**，通过 CSS 变量统一管理颜色。后续阶段的组件应使用这些变量（如 `var(--surface-color)`），不要硬编码颜色值。变量定义在 `src/style.css` 的 `:root` 中。

### 8.6 不要做的事

- **不要**实现登录功能（阶段二）
- **不要**实现任何业务页面（阶段三）
- **不要**添加 UI 组件库（如 Element Plus、Naive UI），当前用纯 CSS
- **不要**创建 Docker 相关文件（阶段五）
- **不要**修改 `anime/` 目录中的文件

---

## 9. 完整项目目录预览

阶段一完成后，项目目录应为：

```
fan-web/
├── docs/
│   ├── 需求文档.md
│   ├── 开发计划.md
│   └── 阶段一交接文档.md          # 本文档
├── anime/                          # 测试用番剧文件（不修改）
│   └── [CheeseAni] Re：Zero ... [67] ... .mkv
├── backend/
│   ├── main.go
│   ├── config.yaml
│   ├── go.mod
│   ├── go.sum
│   ├── config/
│   │   └── config.go
│   ├── database/
│   │   └── db.go
│   ├── models/
│   │   └── models.go
│   ├── handlers/
│   │   └── health.go
│   ├── middleware/
│   │   └── cors.go
│   ├── utils/
│   │   └── response.go
│   └── data/                       # 运行后自动生成
│       └── fan-web.db
└── frontend/
    ├── index.html
    ├── package.json
    ├── tsconfig.json
    ├── tsconfig.node.json
    ├── vite.config.ts
    └── src/
        ├── main.ts
        ├── App.vue
        ├── env.d.ts
        ├── style.css
        ├── api/
        │   └── index.ts
        ├── router/
        │   └── index.ts
        ├── stores/
        │   └── app.ts
        ├── components/
        │   └── AppLayout.vue
        └── views/
            └── HomeView.vue
```
